# Analysis path (CPU only): calibration, statistics, tables and figures.
# These are the versions used to produce the released results.
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2
matplotlib==3.10.7
PyYAML==6.0.2

# Preprocessing and the ICP baseline
open3d==0.19.0
plyfile==1.1

# Training and inference. Installed CPU-only here; the GPU runs used the
# NVIDIA NGC PyTorch 23.08 container (torch 2.1) on A100-SXM4 40 GB.
# Only needed for train.py / predict.py, not for reproducing the tables.
torch==2.10.0
